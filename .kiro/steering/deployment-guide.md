# Deployment guide

The maintained procedure is [docs/deployment.md](../../docs/deployment.md). It covers the current private GitLab/CodeBuild/Lambda release path, artifact verification, rollback, and the separately configured GitHub pipeline.

Use the [README](../../README.md) for preview/test commands, resource identifiers, and monitoring. The original PAT-based setup, automatic GitLab deployment implication, and `LIVE`-alias rollback instructions have been removed because they do not describe the current configuration.
